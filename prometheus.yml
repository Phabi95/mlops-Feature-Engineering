global:
  [cite_start]scrape_interval: 5s # High-resolution polling for real-time monitoring 

scrape_configs:
  - job_name: 'optasia_api'
    static_configs:
      - [cite_start]targets: ['optasia_api:8000'] # Internal container endpoint for metric scraping